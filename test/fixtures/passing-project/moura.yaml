version: 1
sources:
  requirements: [req.md]
  specifications: [spec.md]
verification:
  layers: [unit, integration]
requirements:
  - id: REQ-001
    scenarios:
      - id: SCN-001
        cases:
          - id: CASE-001
            verify: [unit, integration]
